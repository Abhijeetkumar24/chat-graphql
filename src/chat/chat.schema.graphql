type User {
  id: ID!
  username: String!
}

type Message {
  id: ID!
  content: String!
  sender: User!
  receiver: User!
  timestamp: String!
}

type Query {
  getMessage(id: ID!): Message
}

schema {
  query: Query
}
